const controlLoginForm = document.querySelector("#controlLoginForm");const controlPasswordInput = document.querySelector("#controlPassword");const controlLoginError = document.querySelector("#controlLoginError");controlLoginForm.addEventListener("submit", async (event) => {event.preventDefault();controlLoginError.hidden = true;const response = await fetch("/api/control-login", {method: "POST",headers: { "Content-Type": "application/json" },body: JSON.stringify({ password: controlPasswordInput.value }),});if (!response.ok) {controlLoginError.hidden = false;return;}const next = new URLSearchParams(window.location.search).get("next") || "/control";window.location.href = next;});
