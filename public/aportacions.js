const DONATION_URL = "";const donationLink = document.querySelector("#donationLink");const donationNote = document.querySelector("#donationNote");const tournamentNameElements = document.querySelectorAll("[data-tournament-name]");async function initDonations() {const state = await fetch(`/api/state?t=${Date.now()}`, { cache: "no-store" }).then((response) => response.json()).catch(() => null);tournamentNameElements.forEach((element) => {element.textContent = state?.settings?.tournamentName || "Open la Pobla";});if (DONATION_URL) {donationLink.href = DONATION_URL;donationLink.target = "_blank";donationLink.rel = "noreferrer";donationNote.textContent = "Gràcies per ajudar a fer créixer el campionat.";} else {donationLink.href = "/resultats";donationNote.textContent = "L'enllaç d'aportacions encara està pendent de configurar.";}}initDonations();
