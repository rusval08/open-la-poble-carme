const CACHE_NAME = "cpool-public-app-v5";const APP_SHELL = ["/resultats","/estadistiques.html","/quadrant","/tv-table","/styles.css","/resultats-theme.css","/blackball-admin-theme.css","/estadistiques-theme.css","/resultats.js","/estadistiques.js","/quadrant.js","/tv-table.js","/logo.js","/pwa.js","/manifest.webmanifest","/cpool-logo.png","/blackball-blue-bg.png","/trofeus-demo-fons.png",];self.addEventListener("install", (event) => {event.waitUntil(caches.open(CACHE_NAME).then((cache) => cache.addAll(APP_SHELL)));self.skipWaiting();});self.addEventListener("activate", (event) => {event.waitUntil(caches.keys().then((keys) => Promise.all(keys.filter((key) => key !== CACHE_NAME).map((key) => caches.delete(key)))),);self.clients.claim();});self.addEventListener("fetch", (event) => {const requestUrl = new URL(event.request.url);if (requestUrl.origin !== location.origin || requestUrl.pathname.startsWith("/api/")) return;if (event.request.mode === "navigate") {event.respondWith(fetch(event.request).catch(() => caches.match("/resultats")));return;}event.respondWith(caches.match(event.request).then((cached) => {const fetchPromise = fetch(event.request).then((response) => {if (response && response.ok) {const copy = response.clone();caches.open(CACHE_NAME).then((cache) => cache.put(event.request, copy));}return response;}).catch(() => cached);return cached || fetchPromise;}),);});
