const logoutButton = document.querySelector("#logoutButton"); const settingsForm = document.querySelector("#settingsForm"); const tournamentNameInput = document.querySelector("#tournamentNameInput"); const saveSettings = document.querySelector("#saveSettings"); const tournamentNameElements = document.querySelectorAll("[data-tournament-name]"); function applyTournamentName(name) { const value = name || "Open la Pobla"; tournamentNameElements.forEach((element) => { element.textContent = value; }); if (tournamentNameInput) tournamentNameInput.value = value; } async function loadAdminState() { const state = await fetch(`/api/state?t=${Date.now()}`, { cache: "no-store" }).then((response) => response.json()); applyTournamentName(state.settings?.tournamentName); } settingsForm?.addEventListener("submit", async (event) => { event.preventDefault(); const tournamentName = tournamentNameInput.value.trim() || "Open la Pobla"; saveSettings.textContent = "Guardant..."; const response = await fetch("/api/settings", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ tournamentName }), }); if (response.ok) { const data = await response.json(); applyTournamentName(data.settings?.tournamentName); saveSettings.textContent = "Nom guardat"; } else { saveSettings.textContent = "Error"; } setTimeout(() => { saveSettings.textContent = "Guardar nom"; }, 1400); }); logoutButton?.addEventListener("click", async () => { await fetch("/api/logout", { method: "POST" }); window.location.href = "/login"; }); loadAdminState();
