const manualBracket = document.querySelector("#manualBracket"); const saveBracket = document.querySelector("#saveBracket"); const randomTables = document.querySelector("#randomTables"); const editableRounds = [ { key: "r64", title: "Ultims 64", count: 32 }, { key: "r32", title: "Ultims 32", count: 16 }, { key: "r16", title: "Vuitens", count: 8 }, { key: "qf", title: "Quarts", count: 4 }, { key: "sf", title: "Semifinals", count: 2 }, { key: "final", title: "Final", count: 1 }, ]; let bracketManual = {}; let autoSaveTimer = null; function emptyManual() { return Object.fromEntries( editableRounds.map((round) => [ round.key, Array.from({ length: round.count }, () => ({ top: "", bottom: "", table: "", winner: "", score: "", topManual: false, bottomManual: false, })), ]), ); } function tableOptions(value) { return ` <option value="">Sense taula</option> ${[1, 2, 3, 4, 5, 6] .map((table) => `<option value="${table}" ${String(value) === String(table) ? "selected" : ""}>Taula ${table}</option>`) .join("")} `; } function resultOptions(match) { const options = [ ["", "Sense guanyador"], [match.top || "", match.top || "Jugador 1"], [match.bottom || "", match.bottom || "Jugador 2"], ]; return options .map(([value, label], index) => { if (index > 0 && !value) return ""; return `<option value="${value}" ${String(match.winner) === String(value) ? "selected" : ""}>${label}</option>`; }) .join(""); } function matchTitle(roundKey, index) { if (roundKey === "r64") return `Partit ${index + 1}`; if (roundKey === "r32") return `Partit ${index + 33}`; if (roundKey === "r16") return `Partit ${index + 49}`; if (roundKey === "qf") return `Quart ${index + 1}`; if (roundKey === "sf") return `Semi ${index + 1}`; return "Final"; } function renderManualBracket() { manualBracket.innerHTML = editableRounds .map( (round) => ` <section class="manual-round"> <h2>${round.title}</h2> <div class="manual-match-list"> ${bracketManual[round.key] .map( (match, index) => ` <article class="manual-match"> <strong>${matchTitle(round.key, index)}</strong> ${ match.winner ? `<p class="manual-result">Final: ${match.score || ""} - Guanya ${match.winner}</p>` : "" } <label> <span>Taula de joc</span> <select data-round="${round.key}" data-index="${index}" data-side="table"> ${tableOptions(match.table)} </select> </label> <button class="load-table-button play-match-button" data-action="load-table" data-round="${round.key}" data-index="${index}" type="button"> Marcar en joc </button> ${ round.players === false ? "" : ` <label> <span>Jugador 1</span> <input data-round="${round.key}" data-index="${index}" data-side="top" value="${match.top}" placeholder="Pendent" /> </label> <label> <span>Jugador 2</span> <input data-round="${round.key}" data-index="${index}" data-side="bottom" value="${match.bottom}" placeholder="Pendent" /> </label> ` } <div class="manual-result-editor"> <label> <span>Guanyador</span> <select data-round="${round.key}" data-index="${index}" data-side="winner"> ${resultOptions(match)} </select> </label> <label> <span>Marcador final</span> <input data-round="${round.key}" data-index="${index}" data-side="score" value="${match.score}" placeholder="Exemple: 5 - 3" /> </label> <button class="clear-result-button" data-action="clear-result" data-round="${round.key}" data-index="${index}" type="button"> Esborrar resultat </button> </div> </article> `, ) .join("")} </div> </section> `, ) .join(""); } manualBracket.addEventListener("input", (event) => { const input = event.target.closest("[data-round]"); if (!input) return; updateManualField(input); scheduleAutoSave(); }); manualBracket.addEventListener("change", (event) => { const input = event.target.closest("[data-round]"); if (!input) return; updateManualField(input); scheduleAutoSave(); }); async function saveManualBracket() { const response
